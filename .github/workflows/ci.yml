name: integration safety

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  shell-contract:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: bash tests/test_release.sh
